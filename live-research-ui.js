import { researchTarget } from './research-api.js'
const memory=new Map();let inFlightKey=''
function readProfile(){try{return JSON.parse(localStorage.getItem('prepare-profile')||'{}')}catch{return{}}}
function readCompetencies(){return[...document.querySelectorAll('.model-chip-list > span')].map((node)=>({name:node.querySelector('b')?.textContent?.trim()||'',weight:Number.parseFloat(node.querySelector('small')?.textContent||'')||1})).filter((item)=>item.name)}
function readGaps(){return[...document.querySelectorAll('.skill-evidence')].map((node)=>({name:node.querySelector('.skill-row strong')?.textContent?.trim()||'',score:Number.parseInt(node.querySelector('.skill-row > div:first-child span')?.textContent||'',10)||0,priority:Math.max(1,100-(Number.parseInt(node.querySelector('.skill-row > div:first-child span')?.textContent||'',10)||0))})).filter((item)=>item.name)}
function researchKey(profile,competencies,gaps){return JSON.stringify({track:profile.track,company:profile.company,role:profile.role,level:profile.level,grade:profile.grade,subject:profile.subject,examName:profile.examName,district:profile.district,school:profile.school,curriculumTrack:profile.curriculumTrack,degree:profile.degree,branch:profile.branch,semester:profile.semester,graduationYear:profile.graduationYear,companies:profile.companies,programmingLanguages:profile.programmingLanguages,competencies:competencies.map((x)=>[x.name,x.weight]),gaps:gaps.slice(0,8).map((x)=>x.name)})}
const privacyCopy='Profile, answers, and progress remain in your browser. After the diagnostic, Prepare may send target fields plus competency/gap summaries to the live research API; individual diagnostic answers are not sent.'
function updatePrivacyNotice(){const note=document.querySelector('#intake .privacy-note');if(note&&note.textContent!==privacyCopy)note.textContent=privacyCopy}
function createPanel(){let panel=document.querySelector('#live-research-panel');if(panel)return panel;const anchor=document.querySelector('.source-learning-panel');if(!anchor)return null;panel=document.createElement('section');panel.id='live-research-panel';panel.className='panel source-learning-panel';anchor.insertAdjacentElement('afterend',panel);return panel}
function markPanel(panel,key,state){panel.dataset.researchKey=key;panel.dataset.researchState=state}
function text(tag,className,value){const el=document.createElement(tag);if(className)el.className=className;el.textContent=value;return el}
function sourceCard(source){const card=document.createElement('article');card.className='source-card';const meta=document.createElement('div');meta.className='source-meta';meta.append(text('span','',source.quality||'Live public source'),text('span','',`score ${source.score??'—'}`));card.append(meta,text('strong','',source.title||source.publisher||'Source'),text('p','',source.description||source.snippet||'Fresh public result for this preparation target.'));try{const url=new URL(source.url);if(['http:','https:'].includes(url.protocol)){const link=text('a','',`Open ${source.publisher||url.hostname} ↗`);link.href=url.toString();link.target='_blank';link.rel='noopener noreferrer';card.append(link)}}catch{}return card}
function renderLoading(panel,key){markPanel(panel,key,'loading');panel.replaceChildren();const head=document.createElement('div');head.className='source-learning-head';const copy=document.createElement('div');copy.append(text('div','eyebrow','Live target research'),text('h3','','Checking current public sources…'),text('p','support-copy','The reviewed local catalog stays available while Prepare checks current public evidence for this exact target.'));head.append(copy,text('span','source-badge','Researching'));panel.append(head)}
function renderUnavailable(panel,key,message){markPanel(panel,key,'error');panel.replaceChildren();const head=document.createElement('div');head.className='source-learning-head';const copy=document.createElement('div');copy.append(text('div','eyebrow','Live target research'),text('h3','','Reviewed catalog remains active'),text('p','support-copy',message||'Live research is not available right now. No learning flow is blocked.'));head.append(copy,text('span','source-badge','Fallback active'));panel.append(head)}
function renderResearch(panel,key,research){markPanel(panel,key,'ready');panel.replaceChildren();const head=document.createElement('div');head.className='source-learning-head',copy=document.createElement('div');copy.append(text('div','eyebrow','Live target research'),text('h3','',research.target?.label||'Fresh public evidence'));const mode=research.cache?.hit?'Reused from the 30-day research cache':'Freshly researched public results';copy.append(text('p','support-copy',`${mode}. Source authority and target relevance are scored separately; open the original publisher to verify details.`));head.append(copy,text('span','source-badge',`${research.sources?.length||0} live sources`));panel.append(head);const strong=(research.sources||[]).filter((source)=>source.score>=62).slice(0,10);if(!strong.length){panel.append(text('p','support-copy','The live search returned no sufficiently strong sources. Continue with the reviewed catalog above.'));return}const grid=document.createElement('div');grid.className='target-source-list';strong.forEach((source)=>grid.append(sourceCard(source)));panel.append(grid);const qc=research.queryCache?` Query cache: ${research.queryCache.hits}/${research.queryCache.hits+research.queryCache.misses} reused.`:'';panel.append(text('p','support-copy',`Researched ${new Date(research.researchedAt).toLocaleString()}. Provider: ${research.provider?.name||'web search'}.${qc}`))}
async function enhanceResults(){if(!document.querySelector('.results-wrap'))return;const panel=createPanel();if(!panel)return;const profile=readProfile(),competencies=readCompetencies(),gaps=readGaps();if(!profile.track||!competencies.length||!gaps.length)return;const key=researchKey(profile,competencies,gaps),panelState=panel.dataset.researchState;if(panel.dataset.researchKey===key&&['loading','ready','error'].includes(panelState))return;if(memory.has(key)){renderResearch(panel,key,memory.get(key));return}if(inFlightKey===key)return;inFlightKey=key;renderLoading(panel,key);try{const research=await researchTarget(profile,{competencies},gaps);memory.set(key,research);if(document.querySelector('.results-wrap'))renderResearch(createPanel(),key,research)}catch(error){const message=error?.name==='AbortError'?'Live research timed out. Continue with the reviewed sources and retry later.':'Live research service is not reachable right now. Continue with the reviewed sources above.';if(document.querySelector('.results-wrap'))renderUnavailable(createPanel(),key,message)}finally{if(inFlightKey===key)inFlightKey=''}}
let queued=false;const observer=new MutationObserver(()=>{if(queued)return;queued=true;queueMicrotask(()=>{queued=false;updatePrivacyNotice();enhanceResults()})});observer.observe(document.querySelector('#app')||document.body,{childList:true,subtree:true});updatePrivacyNotice();enhanceResults()
